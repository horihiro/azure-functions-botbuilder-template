{
  "name": "{{FUNCTION_BOT}}",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "rimraf": "rimraf",
    "ngrok": "ngrok http 7071",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "azure-function-express": "^2.0.0",
    "botbuilder": "^4.10.4",
    "html-entities": "^1.3.1"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "@ffflorian/jszip-cli": "^3.1.0",
    "@types/node-fetch": "^2.5.7",
    "ngrok": "^3.3.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.3.3"
  }
}
